#include <bits/stdc++.h>
#include <iostream>
using namespace std;

typedef long long ll;
using vi = vector<int>;

void solve()
{
    int n, m;
    cin >> n >> m;

    for (int i = 0; i < n; i++)
    {
        string x;
        cin >> x;
    }
    cout << m;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}